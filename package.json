{
  "name": "select_plugin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "gh-pages": "^3.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  },
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "rm -rf dist/ && npm run build && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KotelnikovIgor/Select_plugin.git"
  },
  "homepage": "https://KotelnikovIgor.github.io/Select_Plugin",
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/KotelnikovIgor/Select_plugin/issues"
  }
}
